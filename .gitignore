# Never commit signing material or local SDK paths
keystore.properties
keys/
local.properties
.gradle/
build/
app/build/
*.apk

# Native speech library (50 MB, one file). Fetch it once with tools/fetch-voice-libs.sh
app/libs/*.aar
*.jks
.kotlin/
*.log
