<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Voice is entirely local: the mic feeds Whisper on the phone, nothing is uploaded. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalChat">

        <!-- Required by LiteRT-LM to load the GPU backend native libraries -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <!--
            Speech synthesis runs in its own process. It is a third-party native stack (Piper,
            espeak-ng, ONNX Runtime) and it was killing the app when it shared a process with the
            language model. Isolated, a crash there costs a sentence, not the conversation.
        -->
        <service
            android:name=".voice.TtsService"
            android:exported="false"
            android:process=":speech" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
